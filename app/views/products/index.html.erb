<% provide(:title, "Products") %>
<h1 class="page-head">Popular Products List</h1>
<h5 style="text-align: center; max-width: 70%; margin: 20px auto; color: red;">Amazon Links are often blocked by adblockers. If you are using an adblocker, please disable it and refresh this page to view products.</h5>
<div class="row">
    <% @products.each do |product| %>
        <div id="<%= " product_#{product.id}" %>" class="large-3 medium-4 small-6 columns">
            <div class="amzn-link"><%= product.image_link.html_safe %></div>
            <p><%= product.text_link.html_safe %></p>
        </div>
    <% end %>
</div>
